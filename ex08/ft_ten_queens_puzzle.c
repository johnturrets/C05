#include <stdlib.h>

int check_diagonal(char* row, int col)
{
	int	i;
	i = 0;
	pos = 6, row[3]
	pos = 5, row[2]
	// row[pos] = posicao na coluna pos
	// check if any on same diagonal
	check_diagonal(row, col + 1)
	while(row[i] != row[col] - col)
		check_puzzle(row, c)

int ft_ten_queens_puzzle(void)
{
	//char (*board) [10] = (char **) malloc(sizeof(int[10][10]));
	while(ft_ten_queens_puzzle())

